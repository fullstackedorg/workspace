{
  "name": "fullstacked",
  "title": "FullStacked",
  "version": "0.10.0-3",
  "description": "A CLI that takes care of all the redundant tasks in TypeScript web app development.",
  "types": "index.d.ts",
  "scripts": {
    "clean": "npx esbuild clean.ts --outfile=clean.js --format=esm && node clean",
    "build": "npm run clean && npx esbuild build.ts --outfile=build.js --format=esm && node build && npx tsc",
    "prepack": "npm run build",
    "pretest": "npx esbuild test.ts --outfile=test.js --format=esm",
    "test": "node test"
  },
  "type": "module",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/cplepage/fullstacked.git"
  },
  "keywords": [
    "webapp",
    "typescript",
    "fullstack",
    "tool"
  ],
  "author": "CP Lepage",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/cplepage/fullstacked/issues"
  },
  "funding": {
    "type": "patreon",
    "url": "https://www.patreon.com/fullstacked"
  },
  "homepage": "https://fullstacked.org",
  "bin": {
    "fullstacked": "cli.js"
  },
  "devDependencies": {
    "@types/dockerode": "^3.3.14",
    "@types/glob": "^8.1.0",
    "@types/node": "^18.14.2",
    "esbuild": "^0.17.10",
    "glob": "^9.1.2",
    "typescript": "^4.9.5"
  },
  "dependencies": {
    "cli-table3": "^0.6.3",
    "dockerode": "^3.3.4",
    "xterm-addon-fit": "^0.7.0"
  },
  "exports": {
    "./commands/info": "./commands/info/index.js",
    "./*": "./*.js"
  }
}
