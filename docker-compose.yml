version: '3.7'

services:
  node:
    image: node:16-alpine
    working_dir: /app
    command: [ "node", "index", "${FLAGS}" ]
    restart: always
    ports:
      - "8000:8000"
    volumes:
      - ./${VERSION}:/app
