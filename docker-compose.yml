version: '3.7'

services:
  node:
    image: node:16-alpine
    working_dir: /app
    command: [ "node", "index", "${FLAGS}" ]
    restart: unless-stopped
    ports:
      - "8000:8000"
    volumes:
      - ./${VERSION}:/app
